"""Сегментация объекта на реальном фото (силуэт-маска) для этапа 2 задачи "3D-реконструкция по
реальным фото через ArUco-метки" — вход для будущего visual hull, наравне с позой камеры
(`pose/`).

Не зависит от Qt/GUI. Сама модель (SAM3) — тяжёлая GPU-зависимость (torch/tensorrt/sam3, ~7 ГБ)
из соседнего проекта `UavVisionLab`, поэтому вызывающий код (`gui/dialogs/photo_capture_dialog.py`)
работает только через `SegmentationBackend` (`base.py`) — конкретный способ получить маску
(сейчас — subprocess к Windows .venv UavVisionLab, `sam3_subprocess_backend.py`) можно поменять,
не трогая диалог. Решение вынести SAM3 в WSL (для последующего переноса в Webots-симулятор)
рассматривалось и отложено отдельным этапом, см. заметку задачи в вики.
"""
